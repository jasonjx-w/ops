#include "worker.h"

// Worker::SetUp() {
//     std::transform(sub_graph_.begin(), sub_graph_.end(), kernels_, )
//     for (int i = 0; i < workers_.size(); ++i) {
//         workers_.SetUp();
//     }
// }
//
// Kernel Worker::NodeToKernel(const Node *node) {
//
// }
